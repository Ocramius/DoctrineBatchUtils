language: php

sudo: false

php:
  - 7.1
  - 7.2
  - 7.3
  - 7.4snapshot
  - nightly

env:
  - DEPENDENCIES="high"
  - DEPENDENCIES="low"

before_script:
  - travis_retry sh .travis.install.sh

script:
  - ./vendor/bin/phpunit --coverage-clover ./clover.xml
  - php examples/persisting-new-objects-in-batch.php > /dev/null
  - php examples/working-with-query-resultsets-in-batch.php > /dev/null
  - ulimit -n 4096 && phpdbg -qrr ./vendor/bin/infection -vvv --min-msi=92 --min-covered-msi=100
